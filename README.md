# Simple Blog

个人技术学习笔记和日常记录的数字花园，使用 [Quartz](https://quartz.jzhao.xyz/) 构建。

## 本地运行

```bash
npm install
npm run serve
```

## 构建

```bash
npm run build
```

构建产物会输出到 `public` 目录。

## 部署

推送到 `main` 分支后，GitHub Actions 会自动构建并部署到 GitHub Pages。
